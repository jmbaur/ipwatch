# IPWatch

Run hooks (code, executables, etc.) on changes to IP addresses.

## Usage

```console
$ ipwatch \
	[-4] [-6] \
	[-filter=FILTER ...] \
	[-interface=IFACE ...] \
	[-max-retries=NUM] \
	[-hook=HOOK ...] \
	[-env=FILE]
```
